#include<bits/stdc++.h>

namespace ranges = std::ranges;
namespace views = std::views;

int main()
{
    #ifndef PAUSE
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    #endif
    
    
    
    #ifdef PAUSE
    std::cout << std::endl;
    system("pause");
    #endif
    return 0;
}